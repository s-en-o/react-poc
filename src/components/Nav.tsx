import { Link } from 'react-router-dom';

const Nav = () => {
    return (
        <>
            <nav>
                <Link to="/parallax">Parallax</Link>
            </nav>
        </>
    );
};

export default Nav;
